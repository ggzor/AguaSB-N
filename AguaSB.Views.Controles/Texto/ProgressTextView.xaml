<UserControl x:Class="AguaSB.Views.Controles.Texto.ProgressTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Name="This">
    <StackPanel>
        <TextBlock x:Name="Title" Text="Title" TextAlignment="Center" TextWrapping="Wrap" Style="{Binding ElementName=This, Path=TitleStyle}"/>
        <TextBlock x:Name="Subtitle" Text="Subtitle" TextAlignment="Center" TextWrapping="Wrap" Style="{Binding ElementName=This, Path=TitleStyle}"/>
    </StackPanel>
</UserControl>
